---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getCollection } from "astro:content";

const pageTitle = "Recipes";
const recipes = await getCollection("recipes");
---

<BaseLayout pageTitle={pageTitle}>
  <p>Explore my favorite recipes and culinary experiments</p>
  <ul class="recipes-grid">
    {
      recipes.map((recipe) => (
        <li class="recipe-card">
          <a
            href={`${import.meta.env.BASE_URL}recipes/${recipe.id}`}
            class="recipe-link"
          >
            {recipe.data.title}
          </a>
        </li>
      ))
    }
  </ul>
  <div class="navigation-button">
    <a href={`${import.meta.env.BASE_URL}illustrations`} class="next-link"
      >Let's check out the illustrations →</a
    >
  </div>
</BaseLayout>

<style>
  .recipes-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(14rem, 1fr));
    gap: 1.5rem;
    padding: 0;
    list-style: none;
    margin-top: 2rem;
  }

  .recipe-card {
    list-style: none;
  }

  .recipe-link {
    font-family: "Aesthetic";
    font-size: 2rem;
    text-transform: uppercase;
    display: block;
    background-color: rgb(166, 1, 59);
    color: white;
    padding: 0.9rem 1.2rem;
    border-radius: 2rem;
    text-decoration: none;
    text-align: center;
    font-weight: 600;
    transition:
      transform 0.15s ease,
      opacity 0.15s ease;
  }

  .recipe-link:hover {
    transform: translateY(-0.2rem);
    opacity: 0.8;
  }

  html.dark .recipe-link {
    background-color: #f9f6f1;
    color: rgb(28, 0, 10);
  }

  html.dark .recipe-link:hover {
    background-color: #f9f6f1;
    color: rgb(28, 0, 10);
    opacity: 0.8;
  }

  .navigation-button {
    margin-top: 3rem;
    text-align: center;
  }

  .next-link {
    display: inline-block;
    font-family: "Aesthetic";
    font-size: 1.2rem;
    text-transform: uppercase;
    background-color: rgb(166, 1, 59);
    color: white;
    padding: 0.6rem 0.9rem;
    border-radius: 2rem;
    text-decoration: none;
    text-align: center;
    font-weight: 600;
    transition:
      transform 0.15s ease,
      opacity 0.15s ease;
  }

  .next-link:hover {
    transform: translateY(-0.2rem);
    opacity: 0.8;
  }

  html.dark .next-link {
    background-color: #f9f6f1;
    color: rgb(28, 0, 10);
  }

  html.dark .next-link:hover {
    background-color: #f9f6f1;
    color: rgb(28, 0, 10);
    opacity: 0.8;
  }
</style>
