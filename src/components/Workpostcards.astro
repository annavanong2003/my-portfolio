---
import { getCollection } from "astro:content";

const posts = await getCollection("posts");
---

{
  posts.map((post) => (
    <li>
      <a href={`${import.meta.env.BASE_URL}posts/${post.id}`}>{post.id}</a>
    </li>
  ))
}
